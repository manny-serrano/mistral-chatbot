<?xml version="1.0" encoding="UTF-8"?>
<SPConfig xmlns="urn:mace:shibboleth:3.0:native:sp:config"
    xmlns:conf="urn:mace:shibboleth:3.0:native:sp:config"
    xmlns:md="urn:oasis:names:tc:SAML:2.0:metadata"
    clockSkew="180">

    <OutOfProcess tranLogFormat="%u|%s|%IDP|%i|%ac|%t|%attr|%n|%b|%E|%S|%SS|%L|%UA|%a" />

    <!-- The ApplicationDefaults element is where most of Shibboleth's SAML bits are defined. -->
    <ApplicationDefaults entityID="https://levantai.colab.duke.edu"
                         REMOTE_USER="eppn persistent-id targeted-id"
                         encryption="false"
                         signing="false"
                         signingAlg="http://www.w3.org/2001/04/xmldsig-more#rsa-sha256"
                         digestAlg="http://www.w3.org/2001/04/xmlenc#sha256">

        <!-- Controls session lifetimes, address checks, cookie handling, and the protocol handlers. -->
        <Sessions lifetime="28800" timeout="3600" relayState="ss:mem"
                  checkAddress="false" handlerSSL="true" cookieProps="https"
                  redirectLimit="exact" redirectWhitelist="https://shib.oit.duke.edu/ https://levantai.colab.duke.edu/">

            <!-- Default SSO location - Points to Duke IdP with official URLs -->
            <SSO entityID="https://shib.oit.duke.edu/shibboleth-idp">
                SAML2 SAML1
            </SSO>

            <!-- SAML and local-only logout. -->
            <Logout>SAML2 Local</Logout>

            <!-- Administrative functions -->
            <Handler type="MetadataGenerator" Location="/Metadata" signing="false"/>
            <Handler type="Status" Location="/Status" acl="127.0.0.1 ::1"/>
            <Handler type="Session" Location="/Session" showAttributeValues="true"/>

            <!-- JSON feed of discovery information. -->
            <Handler type="DiscoveryFeed" Location="/DiscoFeed"/>
        </Sessions>

        <!-- Allows overriding of error template information/filenames. -->
        <Errors supportContact="admin@duke.edu"
                helpLocation="/about.html"
                styleSheet="/shibboleth-sp/main.css"/>

        <!-- Multiple metadata providers for redundancy -->
        <!-- Primary: Local file metadata provider -->
        <MetadataProvider type="XML" validate="false" file="duke-metadata-3-signed.xml"/>
        
        <!-- Fallback: URL-based metadata provider -->
        <MetadataProvider type="XML" validate="false"
                          url="https://shib.oit.duke.edu/duke-metadata-3-signed.xml"
                          backingFilePath="duke-metadata-fallback.xml"
                          reloadInterval="7200"/>
        
        <!-- Emergency fallback: Minimal metadata provider -->
        <MetadataProvider type="XML" validate="false">
            <Inline>
                <md:EntityDescriptor entityID="https://shib.oit.duke.edu/shibboleth-idp" xmlns:md="urn:oasis:names:tc:SAML:2.0:metadata">
                    <md:IDPSSODescriptor protocolSupportEnumeration="urn:oasis:names:tc:SAML:2.0:protocol">
                        <md:SingleSignOnService Binding="urn:oasis:names:tc:SAML:2.0:bindings:HTTP-Redirect" Location="https://shib.oit.duke.edu/idp/profile/SAML2/Redirect/SSO"/>
                        <md:SingleSignOnService Binding="urn:oasis:names:tc:SAML:2.0:bindings:HTTP-POST" Location="https://shib.oit.duke.edu/idp/profile/SAML2/POST/SSO"/>
                    </md:IDPSSODescriptor>
                </md:EntityDescriptor>
            </Inline>
        </MetadataProvider>

        <!-- Map to extract attributes from SAML assertions. -->
        <AttributeExtractor type="XML" validate="true" reloadChanges="false" path="attribute-map.xml"/>

        <!-- Default filtering policy for recognized attributes, lets other data pass. -->
        <AttributeFilter type="XML" validate="true" path="attribute-policy.xml"/>

        <!-- Simple file-based resolvers for separate signing/encryption keys. -->
        <CredentialResolver type="File" use="signing" 
                            key="sp-signing-key.pem" 
                            certificate="sp-signing-cert.pem"/>
        <CredentialResolver type="File" use="encryption" 
                            key="sp-encrypt-key.pem" 
                            certificate="sp-encrypt-cert.pem"/>

        <!-- The default settings can be overridden by creating ApplicationOverride elements. -->

    </ApplicationDefaults>

    <!-- Policies that determine how to process and authenticate runtime requests. -->
    <SecurityPolicyProvider type="XML" validate="true" path="security-policy.xml"/>

    <!-- Low-level configuration about protocols and bindings available for use. -->
    <ProtocolProvider type="XML" validate="true" path="protocols.xml"/>

</SPConfig>
